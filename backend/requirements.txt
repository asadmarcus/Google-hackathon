# CIRO Backend — Requirements
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0
httpx==0.27.0

# ML (for Agent 3 — install now so it's ready)
scikit-learn==1.5.0
xgboost==2.1.0
joblib==1.4.0
pandas==2.2.0
numpy==1.26.0

# Firebase (uncomment when credentials ready)
# firebase-admin==6.5.0

# Utilities
python-dotenv==1.0.1
python-multipart==0.0.9
